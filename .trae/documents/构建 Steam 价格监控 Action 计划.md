根据您的反馈，我优化了实施方案，增加了多游戏支持并调整了定时策略。

### 优化后的实施方案

#### 1. 核心监控脚本 (`monitor.py`) - 支持多游戏
*   **App ID 数组**：将使用一个列表（数组）来存储 App ID，例如 `APP_IDS = ["3472040"]`。这样您以后只需在列表中添加新的 ID 即可监控其他游戏。
*   **批量查询**：脚本将遍历该列表，依次查询每个游戏的价格信息。
*   **结果输出**：清晰打印每个游戏的名称、当前价格和折扣状态。

#### 2. 自动化工作流 (`.github/workflows/steam_price_monitor.yml`) - 每日定时
*   **触发时间**：设置为**每天北京时间上午 10:00**（或者您指定的其他时间）。
    *   *技术说明*：GitHub Actions 使用 UTC 时间。为了在 Beijing Time (UTC+8) 10:00 触发，我们将 Cron 表达式设置为 `0 2 * * *` (即 UTC 02:00)。
*   **手动触发**：保留手动触发按钮，方便随时测试。

#### 3. 依赖配置 (`requirements.txt`)
*   保持不变，包含 `requests`。

### 总结
这个方案将具备更好的扩展性（支持多个游戏）和确定性（固定时间运行）。如果确认无误，我将开始编写代码。
